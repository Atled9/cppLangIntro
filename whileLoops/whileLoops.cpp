#include <iostream>

int main() {
    /*
    std::string name;
    //the while loop will  not exit until a name is entered
    while (name.empty()) {
        std::cout << "Enter your name: ";
        std::getline(std::cin, name);
    }

    std::cout << "Hello " << name << '\n'; */

    while (1) {
        std::cout << "HELP! I am stuck in an infinite loop!!\n";
    }
    return 0;
}
